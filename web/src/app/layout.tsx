// This root layout is not used because we have a [locale] layout
// The [locale] layout handles everything
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
